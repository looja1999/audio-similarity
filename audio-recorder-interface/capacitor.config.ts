import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'audio-recorder-interface',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
